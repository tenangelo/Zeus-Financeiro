name: Deploy Zeus Financeiro

on:
  push:
    branches:
      - main

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deployment Info
        run: |
          echo "✅ Zeus Financeiro Deployment Pipeline"
          echo "Commit: ${{ github.sha }}"
          echo "Branch: ${{ github.ref }}"
          echo ""
          echo "📦 Services:"
          echo "  - Web: https://zeus-web-production.up.railway.app"
          echo "  - API: https://zeusapi.railway.internal"
          echo "  - Frontend: https://zeus-financeiro-mw1ycfa73-angelo-goncalves-projects.vercel.app"
